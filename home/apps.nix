{ pkgs, ... }:

{
  # LLM agents — managed via brew for fast updates, declared here so
  # homebrew brews are kept in sync by nix-darwin.
  # These are listed in hosts/macbook.nix under homebrew.brews.
}
