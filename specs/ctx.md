# Current Task Context: Adopt pi-subagents + pi-lens + cc-safety-net
State: in progress
## Plan
- [x] `modes/readonly.ts`: add `rust-analyzer` to `CLI_INSTRUMENTS` (source: rustup — verified accurate: nix rustup ships the proxy, stable toolchain has the component, PATH resolves via /etc/profiles). `healthcheck/index.ts`: "pi packages" section added (pi-subagents/pi-lens via settings.json `packages`; cc-safety-net via config-dir + settings mention detection).
- [x] `justfile`: new **`pi-setup`** recipe — converge pi runtime packages, tab-indented, idempotent (`pi install npm:pi-mcp-adapter` + `pi install npm:pi-subagents` + `pi install npm:pi-lens` + `npx -y cc-safety-net@latest install`; `@latest` required by its README; pi-mcp-adapter included deliberately — the recipe is the repo's declarative pi-package list). rust-analyzer stays out — already declarative (nix rustup proxy + stable toolchain component, verified on PATH).
- [ ] Hand over (user-run): `just pi-setup` (npm may still prompt to approve `@ast-grep/cli` lifecycle scripts) → confirm **Standard** preset (`npx -y cc-safety-net@latest status`) → `sudo darwin-rebuild switch --flake .#$(scutil --get LocalHostName)` (deploys the step-1 healthcheck/readonly edits). Packages are runtime-managed — no nix wiring.
- [ ] Verify (fresh session): `/subagents-doctor` healthy; one real "use scout …" delegation (expect child on `glm-5.3-flash` via modes binding); `/lens-health` green + diagnostics appear on a `.rs` edit; `npx cc-safety-net status`/`doctor` ready and a deliberate `git reset --hard` attempt is blocked with reason; composition — modes gates + footer behave, research-mode allowlisted commands don't trip the safety-net parser; `/healthcheck` — new rows green incl. `rust-analyzer` (no install step: verified working via rustup proxy → stable toolchain component).
- [ ] Docs: CHANGELOG adoption block; `specs/overview.md` pi section gains the package list (pi-mcp-adapter, pi-subagents, pi-lens, cc-safety-net + purpose); `specs/ideas.md` — archive the "Rust LSP extension for pi" entry (resolved by pi-lens adoption); README bootstrap gains a `just pi-setup` line (fresh machines converge pi packages with one command).
## Findings
- Decisions: cc-safety-net preset **Standard**; healthcheck rows **now**; pi-subagents config **defaults first, tune after real usage**.
- `pi-subagents` (nicopreme, 441K/mo, v0.70, updated daily; author of our pi-mcp-adapter): builtin roles scout/researcher/evidence-auditor/worker/reviewer/oracle/delegate; foreground+background children, FleetView, per-agent models; children inherit global extensions → boot in implement → **flash** automatically.
- `pi-lens` (95K/mo, v4.2.1): LSP diagnostics/navigation, linters/formatters on write/edit, read-guard + git-guard (`tool_call` hooks — watch composition with modes + safety-net). Node ≥22.19 required — we have v26.9.0 ✓. **rust-analyzer NOT on PATH** (toolchain-local only) → `rustup component add rust-analyzer`.
- `cc-safety-net` (Coding CLI Safety Net): explicit **Pi integration**; parser-based blocks (git reset --hard, force-push, rm -rf targets, incl. inside `bash -c`/`python -c`) + secret-file protection across shell AND read/edit/write tools (SSH keys, .env, ~/.aws) — implement-mode coverage the modes gate lacks. Standard/Strict/Paranoid presets; `status`/`doctor`/`explain`/`logs`; audit trail local.
- Risk: exact cc-safety-net pi footprint (where its hook/config lives) unverified — detection method adjusted during implementation if needed.
## Context
- Files: `dots/pi/agent/extensions/modes/readonly.ts`, `dots/pi/agent/extensions/healthcheck/index.ts`, `specs/overview.md`, `specs/ideas.md`, `CHANGELOG.md`. No new files; no darwin-rebuild needed for the packages themselves (but the healthcheck/readonly edits DO need the usual switch).
- Apply loop is user-run: installs above, then `sudo darwin-rebuild switch --flake .#$(scutil --get LocalHostName)` for the extension edits.
## Next
Justfile recipe in — handover given: user runs `just pi-setup` + switch, then the fresh-session verify round (doctor/scout/lens/safety-net/composition/healthcheck); docs round after that.
