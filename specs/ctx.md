# Current Task Context: Pi harness setup (modes, ~/.pi sync, z.ai MCP, pageinfo)
State: in progress (modes extension live; hardening round planned)

## Plan
- [x] Write `modes` extension (`dots/pi/agent/extensions/modes.ts`): `/mode [research|plan|implement]` + picker, tool-swap via `setActiveTools` with save/restore, bash allowlist gate (research/plan), plan-mode `edit`/`write` limited to `specs/**`, footer status, `appendEntry`/`session_start` persistence, per-mode banner via `before_agent_start`. Model: pi's official `examples/extensions/plan-mode` — drop its plan-execution/`[DONE:n]` machinery (specdev owns planning state). Done: loads clean (`pi -e` smoke test), allowlist unit-checked (37+26 cases).
- [x] Wire dotfiles → `~/.pi/agent` via home.file symlinks: `extensions/`, `keybindings.json` (and `themes/`, `prompts/`, `mcp.json` as they gain content). `settings.json` stays manual (`pi install` / `/settings` write to it). Done: switch applied — both files resolve into the nix store; remaining files wire up as they gain content.
- [x] Harden `modes.ts` bash gate — `maskShellQuoting()` preprocessor implemented per design (single-quoted → fully masked; double-quoted masked but `$` `` ` `` `(` `)` stay visible so substitution stays rejected; escapes masked; line continuations dropped; unquoted `#`-word → comment stripped to EOL keeping the newline; unterminated quote/trailing backslash → reject). Pipe-split + REJECT metachars + allowlist prefix + per-segment curl check run on the MASKED string; find-action/`date -s/--set`/curl-flag checks run quote-tolerant on the RAW command (closes quoted-flag evasion like `find "-exec"`). Block-reason copy fixed (read-only pipelines allowed; nudges quoting metachar args); stray `,,` sparse hole removed.
- [x] Unit-test the gate: pure functions named-exported; `modes.test.ts` co-located in dots (NOT symlinked — pi never loads it); 122 tests green under `node --test` (node 26 native TS stripping): this session's false positives, quoted-literal class, substitution/backtick/quoted-flag must-blocks (`$(...)` incl. inside double quotes, backticks, `| sh`, `tee`, redirects, `;`/`&&` chains, `find -exec` raw and quoted, `date -s` raw and quoted, unterminated quote, empty pipe segments), prior regression corpus, specs-path table. Importing the module in tests doubles as a load smoke test.
- [ ] Hand over switch (store-copy symlink refresh) + `/reload` → live re-probe of the exact commands that false-positived this session; fold checklist.md tests 3 & 5 into this re-probe.
- [ ] Add z.ai `web-search` / `web-reader` to `~/.pi/agent/mcp.json` (pi-specific layer), `headers` via `${ZAI_API_KEY}`.
- [ ] Add `pginf` (pageinfo-rs) via local flake build (`rustPlatform.buildRustPackage` + `fetchCrate`); not in nixpkgs, no upstream releases — compile once per version bump.
- [ ] End-to-end verify: fresh pi session — mode gates behave, banner/status render, resume keeps mode, MCP tools reachable.
- [ ] Scope call: rust LSP (`@narumitw/pi-lsp`) and Symposium crate-matched skills — decided: deferred to a later pass (see Scope decisions finding).
- [x] *(side task: zellij)* Add `zellij` to `brews` in `hosts/macbook.nix` (after `tmuxp`). Rationale: nixpkgs dropped zellij entirely (only `zellijPlugins` remains, verified at our pin and master); brew stable is at 0.45.0 = GitHub latest; fits the fast-moving-CLIs-as-brews doctrine; freshness via manual `brew upgrade zellij`. Done: line added; update later with `brew upgrade zellij` (switch installs missing only).
- [x] *(side task: zellij)* Amend the now-false "tmux only. zellij out of scope" decision lines in `specs/overview.md` (Design decisions) and `README.md` (Design decisions) → "tmux primary; zellij via brew". Config wiring (`dots/zellij.kdl` → `~/.config/zellij/`) deliberately NOT wired this pass — legacy file, revisit if zellij sticks. Done: both lines amended.

## Findings
- **Research mode definition:** figure out the problem → survey related materials (code, docs, web) → discuss with user. Tools: `read`/`grep`/`find`/`ls` + allowlisted bash (`git log|diff|show`, `cargo metadata|tree`, `pginf`). No `edit`/`write`.
- **No fitting third-party:** `@sion10032/pi-modes` (2 Codex-style modes, `<proposed_plan>` ceremony), `@tungthedev/pi-extensions` (kitchen sink), `@gotgenes/pi-permission-system` (gate, not modes). Official plan-mode example implements every needed mechanism → write our own, est. ~250–300 LOC.
- **APIs verified in docs:** `pi.setActiveTools()`, `tool_call` → `{block, reason}`, `appendEntry` + `session_start` restore, `ctx.ui.setStatus`, `before_agent_start` message injection, `context` filter for stale banners; extensions are TS loaded directly, `/reload` hot-reloads.
- **`~/.pi/agent` split:** config (settings/themes/extensions/prompts/mcp.json) vs state (auth/trust/sessions/npm/mcp-cache) → symlink individual files, never the dir; adapter never rewrites source `mcp.json` → symlink-safe.
- **pageinfo-rs:** Alex's own crate; upstream packaging (flake/releases/bininstall) deferred to him. Binary is `pginf`; value = `wreq` browser-impersonating fetches (blocks that stop curl); author ships a skill in-repo.
- **Micro-choices resolved at impl:** mode cycle = **Shift+Tab** primary (Codex/Claude Code convention; thinking-cycle remapped to `ctrl+alt+t` via `~/.pi/agent/keybindings.json`, symlinked from dots — pi never writes that file) + `Ctrl+Alt+M` secondary. `/mode` no-arg shows `ui.select` picker. Default mode on fresh session = `implement` (stock pi start). Bash gate: single-level pipes allowed when every segment is allowlisted (`git log | head` ✓, `cat x | tee y` ✗); `echo`/`printf` allowlisted; `git branch`/`tag` bare-list forms only (bare `git branch NAME` creates). MCP/other-extension tools stay active in all modes — only built-in `edit`/`write` are ever swapped.
- **No `modes` skill (decision after discussion):** the user switches modes, portability to other agents is unneeded, and skill-body delivery is unreliable (depends on the model choosing to load it). `MODE_BANNERS` in the extension is the single source of mode instructions — injected every turn via `before_agent_start` (deterministic). Context filter keeps only the latest current-mode banner, so no accumulation. `implement` mode also got a banner (follow specs/ctx.md checkboxes, stay in scope, verify).
- **Research-mode contract (user's formulation):** figure out the proper solution *before* any plans — analyze the situation in general → survey related materials → formalize demands → establish clear goals → discuss with the user until agreement. Captured verbatim-ish in the research banner.
- **Flake-visibility gotcha (hit in checklist round 1):** `dots/pi/` was untracked → flake eval (git-filtered source) couldn't see it → home-manager installed **dangling symlinks** for `modes.ts` + `keybindings.json` with no build error (symptom: no `/mode`, no Shift+Tab cycle — extension never loaded). Fix: `git add -N dots/pi`; verified via `nix eval ...home.file."...".source` → store copy now contains both files. Durable note added to overview.md Operational notes.
- **Live test, session 2 (post-switch, extension active):** correct blocks: `&&` chains, `2>/dev/null` redirect, `cargo search` (unlisted). False positives — one root cause: all checks run on the raw string, blind to quoting: `grep -n "a\|b" f` (quoted `\|` mangled by naive `split("|")`), quoted `;`/`&&` (REJECT sees chars inside strings), unquoted `grep a\|b` (escaped pipe ≠ pipeline). Also: block message wrongly claims "no pipes" though read-only pipelines pass; stray `,,` in READONLY_PATTERNS (sparse hole, harmless).
- **Gate-fix design:** quote/escape-aware masking before all structural checks — shell-faithful: `"a\|b"` and `a\|b` are literal-pipe args (allowed), `"a; b"` is one echo arg (allowed), `"$(rm)"` executes (still rejected: `$(`/backtick stay visible from double quotes), `'$(rm)'` is literal (allowed). find-action/`date -s` tokens checked on RAW (quoted-flag evasion closed). Accepted tradeoff: masker is content-blind elsewhere (e.g. `date "-s"` slips the flag check) — consistent with heuristic-guardrail, not-sandbox doctrine.
- **Scope decisions (user, this session):** tmux out of scope — running server can't restart; Shift+Tab/extended-keys verify deferred (optional later: user-run `tmux source-file ~/.tmux.conf`, no session loss). Order after modes hardening: z.ai `mcp.json` → `pginf` → e2e verify. Rust LSP + Symposium deferred to a later pass.
- **Approve-edits for implement mode (deferred to ideas.md):** 4-position cycle (research → plan → implement auto → implement approve), edit/write only, whole-change preview required; revisit after real Pi usage — details + npm survey in `specs/ideas.md`.
- **Iteration-loop gotcha:** `~/.pi/agent/extensions/modes.ts` symlinks to the **nix store copy** — repo edits don't reach the live session until the user re-runs the switch; `/reload` then hot-reloads. Loop: patch → `node --test` corpus → hand over switch → `/reload` → live re-probe.

## Context
Mode table: research = read/grep/find/ls + gated bash, edit/write disabled; plan = all tools + `edit`/`write` path-gated to `specs/**`; implement = all tools, no bash gate. This session runs the extension live (implement mode active; hardening lives in the worktree but NOT yet in the live session — the store copy refreshes only on user-run switch). checklist.md: tests 1–2 resolved; 3 & 5 fold into the post-fix live re-probe; 4 — specs-allowed side verified, block-side still to probe; 6 (resume persistence) belongs to e2e verify. tmux `extended-keys` deferred per scope decision. Known accepted gaps (heuristic, not sandbox): quoted args hiding non-token-list flags (`date "-s"`-class); awk/sed scriptability (`system()`, internal redirects) — pre-existing, documented.

## Next
User runs `sudo darwin-rebuild switch --flake .#$(scutil --get LocalHostName)` (one switch covers both: refreshes modes.ts store copy AND installs zellij once its brew line lands), then `/reload` in this session → agent re-probes the exact commands that false-positived (folds in checklist tests 3 & 5). After modes hardening verifies live: z.ai `mcp.json` → `pginf` flake build → e2e verify. zellij updates after install: `brew upgrade zellij` (repo doctrine: switches install missing only, `upgrade = false`).
